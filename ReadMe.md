# Weather app

## First run

Create ./.env and ./laravel/.env

``` bash
    make up
``` 

>> if have error in run make program, run sudo apt install make

``` bash
    make vendor
``` 

``` bash
    make key
``` 

``` bash
    make fresh
``` 